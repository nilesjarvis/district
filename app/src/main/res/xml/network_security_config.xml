<network-security-config>
    <!--
      Self-hosted Jellyfin servers are commonly reached over plain HTTP on a LAN, so cleartext
      is permitted. Point the app at an HTTPS server when exposing it beyond a trusted network.
    -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
